{
  "name": "sdk-experimental-1",
  "version": "0.0.0",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "build": "lerna run --stream build",
    "test": "lerna run test",
    "clean": "lerna run --parallel clean"
  },
  "devDependencies": {
    "jest": "^26.4.2",
    "lerna": "^3.22.1",
    "npm-run-all": "^4.1.5",
    "rimraf": "^3.0.2",
    "ts-jest": "^26.4.1",
    "typescript": "^4.0.3"
  },
  "workspaces": [
    "packages/*"
  ]
}
